import {Directive, ElementRef, Renderer, Input} from 'angular2/core';

@Directive({
  selector: '[{{name | kebabCase}}]',
  host: {
    //'(mouseenter)': 'onMouseEnter()'
  }
})

export class {{name | camelCase}} {


}